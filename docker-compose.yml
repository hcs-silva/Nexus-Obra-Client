services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:5005}
    container_name: nexus-obra-client
    restart: unless-stopped
    ports:
      - "8080:80"